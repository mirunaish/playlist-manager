export const tagCache = {
  valid: false,
  data: {}, // record of tags
};

export const artistCache = {
  valid: false,
  data: {}, // record of artists
};

export const trackCache = {}; // { url: { track: Track | null, valid: bool } }
